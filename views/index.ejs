<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="styles/main.css"> 
  <title>Random Cocktail</title>
</head>

<body>
  <div class="container ">
    

    <%if (locals.data) {%>

      <div class="">
        <h1><%=data.drinks[0].strDrink %></h1>
      </div>

      
      <div class ="ingredients">
        <h2 class="py-3">Ingredients:</h2>
        <ul>
          <% for (let i = 1; i <= 15; i++) { %>
            <% const ingredient = data.drinks[0]["strIngredient" + i]; %>
            <% if (ingredient) { %>
              <li><%= ingredient %></li>
            <% } %>
          <% } %>
        </ul>
      </div>

      <div class ="instructions">
        <h2 class="py-3 container">Instructions:</h2>
        <p><%=data.drinks[0].strInstructions %></p>
      </div>
     
      <% } else { %>
        <h1>Let's find something for you to drink ✌️</h1>
    <% } %>


    <div class ="container">
      <form action="/submit" method="POST">
        <input type="submit" value="Generate Cocktail" class="custom-button">
      </form>
    </div>
     

  </div>

 

  <%if (locals.error) {%>
    <div id="tag-error" class="tag-error">
      <%=error%>
    </div>
    <%}%>

      

    

   
  

</body>

</html>